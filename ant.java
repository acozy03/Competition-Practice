import java.util.*;
public class ant {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in); 
        int n = scanner.nextInt(); 
        for (int i = 0; i < n; i++) {
            int numHills, numTunnels; 
            numHills = scanner.nextInt();
            numTunnels = scanner.nextInt();
            scanner.nextLine();
            for(int j = 0; j < numTunnels; j++) {
                int x,y,cost; 
                x = scanner.nextInt(); 
                y = scanner.nextInt();
                cost = scanner.nextInt();
                
            }
        }
        scanner.close();
    }  
}

class graph {

}
